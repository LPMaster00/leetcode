class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        i, k = 0, 1
        val = nums[0]
        while i < len(nums):
            if nums[i] != val:
                nums[k] = nums[i]
                val = nums[k]
                k += 1
            i += 1
        return k
